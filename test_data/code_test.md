# Inline Code Formatting Test

In this document, the `main()` function calls `run_tool()`.

Here is some **bold text** and a [link to Google](https://google.com) and some `inline code`.

Another paragraph with `another.variable_name`.
